apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
spec:
  selector:
    app: flask-app
  ports:
    - protocol: TCP
      port: 80           # Port exposed inside the cluster
      targetPort: 5000   # Flask app’s container port
  type: LoadBalancer    # Exposes service externally (if supported)

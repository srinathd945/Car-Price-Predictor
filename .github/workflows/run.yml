name: CI/CD with Minikube Deployment

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirement.txt

      - name: Run linter (optional)
        run: |
          echo "Add linter or tests if needed"

  #build-and-publish:
    #needs: build-and-test
    #runs-on: ubuntu-latest
    #steps:
      #- name: Checkout code
       # uses: actions/checkout@v3

      #- name: Set up Docker Buildx
        #uses: docker/setup-buildx-action@v2

      #- name: Login to DockerHub
        #uses: docker/login-action@v2
        #with:
          #username: ${{ secrets.DOCKER_USERNAME }}
          #password: ${{ secrets.DOCKER_PASSWORD }}

      #- name: Build and push Docker image
        #uses: docker/build-push-action@v4
        #with:
          #context: .
          #file: ./Dockerfile
          #push: true
          #tags: ${{ secrets.DOCKER_USERNAME }}/mlops-lr-app:latest

  #deploy-to-minikube:
    #needs: build-and-publish
    #runs-on: self-hosted
    #steps:
      #- name: Checkout code
        #uses: actions/checkout@v4

      #- name: Set kubectl context to Minikube
        #run: |
          #kubectl config use-context minikube

      #- name: Deploy to Kubernetes
        #run: |
          #kubectl apply --validate=false -f k8s/main-deployment.yaml

      #- name: Wait for deployment rollout
        #run: |
         # kubectl rollout status deployment/flask-app-deployment -n main
